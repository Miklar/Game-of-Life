describe("New world", function(){
	/*it("is empty when created", function(){
		expect(worldCreator.create().map).not.toContain(1);
	});

	it("calculates map size by dividing width with tileSize", function(){
		var newWorld = worldCreator.create({widthInPixels: 100, tileSizeInPixels: 10});
		expect(newWorld.getWidth()).toEqual(10);
		var newWorld = worldCreator.create({widthInPixels: 500, tileSizeInPixels: 5});
		expect(newWorld.getWidth()).toEqual(100);
	});

	it("has the given height", function(){
		var newWorld = worldCreator.create({heightInPixels: 210, tileSizeInPixels: 3});
		expect(newWorld.getHeight()).toEqual(70);
	});
	*/
});

